spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/student_management_system
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
#  main:
#    web-application-type: none # disables web server
  profiles: # dev, prod yaml's
    active: dev
#server:
#  port: 8080

json_placeholder:
  base_url: https://jsonplaceholder.typicode.com

