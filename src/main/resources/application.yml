spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tutor_app
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  autoconfigure:
    exclude: #
#    main:
#      web-application-type: none # disables web server
#      lazy-initialization: true # Initializes when they need
  profiles: # dev, prod yaml's
    active: dev # dev, prod comma seperated value
#server:
#  port: 8080

json_placeholder:
  base_url: https://jsonplaceholder.typicode.com

developer:
  mode: ${spring.profiles.active}
  name: dinesh